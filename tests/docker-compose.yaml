services:
  telnet-server:
    image: wistic/telnetd
    ports:
      - "2323:23"
    networks:
      - telnet-net

networks:
  telnet-net:
    enable_ipv6: true

## telnet localhost 2323
## user:password